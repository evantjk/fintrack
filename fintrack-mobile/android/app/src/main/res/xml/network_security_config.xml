<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plaintext HTTP by default (API 28+). This narrowly allows it
  for the local-dev fintrack-api server only (10.0.2.2 is the Android
  emulator's alias for the host machine's localhost) - see
  proceeding_plan.md Stage 2. Once the API is deployed behind HTTPS, this
  file and its reference in AndroidManifest.xml can be removed.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
